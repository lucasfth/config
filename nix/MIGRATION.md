# Nix Migration — Status & Remaining Work

Migration from Homebrew → nix-darwin + home-manager. Started 2026-05-31, completed 2026-06-03.

## Done

- [x] Nix installed, flakes enabled, nix-darwin booted
- [x] nix-darwin + home-manager integrated in `flake.nix`
- [x] ~50 CLI tools migrated from brew to Nix (`packages/`)
- [x] Brew uninstalled all packages now managed by Nix
- [x] Brew remaining packages managed declaratively (`homebrew/ directory`)
- [x] All dotfiles symlinked from Nix store (starship, ghostty, tmux, vim, aerospace, lazygit)
- [x] `.zshrc` generated by home-manager from `shell/` modules (no manual dotfile)
- [x] Shell switched to Nix zsh (`chsh -s ~/.nix-profile/bin/zsh`)
- [x] GPG signing configured, gpg-agent with SSH support
- [x] `nrs` alias, `nix-search`, `nix-which`, `nix-update`, `nix-rollback`
- [x] Secrets gitignored: `nix_secrets`, `.claude/`, `.omp/agent/*.db*`, `node_modules/`
- [x] `repolicense-cli` added as git submodule
- [x] `Brewfile` deleted (brew managed by Nix now)
- [x] `README.md` written with quick-reference commands

## Remaining TODOs (your decisions)

### Services (Postgres & Redis)

Currently running as **brew services** (`brew services start postgresql@14`, `brew services start redis`). They're in `homebrew/` so Nix keeps them installed but doesn't manage the service lifecycle.

To migrate to Nix services, uncomment in `nix/darwin/services.nix`:
```nix
services.postgresql = { enable = true; package = pkgs.postgresql_14; };
services.redis = { enable = true; };
```

Then dump data, stop brew services, rebuild. See original MIGRATION.md step 2.1 for the full dump/restore procedure.

**Status:** Deliberately deferred. Not urgent.

### Dock apps

`nix/darwin/system.nix` has empty `persistent-apps`. Fill in:
```bash
defaults read com.apple.dock persistent-apps | grep -o '".*\.app"'
```

### SDKMAN

Commented out in `shell/` init. If you use `sdk` to manage Java versions, uncomment the two lines. Otherwise `pkgs.jdk21` from Nix covers you.

### Anaconda

Commented out in `shell/` init. If you still need it, uncomment the PATH line.

### yt-dlp

Lives at `~/Desktop/code/yt-dlp/` — a manual clone, not in git. The alias in `shell/aliases.nix` is guarded with `[ -x ... ]` so it silently skips on new machines. Consider:
- Adding as a git submodule (like repolicense-cli)
- Or using the Nix version: add `yt-dlp` to `packages/`

### macOS defaults

`nix/darwin/system.nix` covers common settings. You may have custom overrides not captured. Check:
```bash
defaults read | grep -E "(NSGlobalDomain|com.apple.dock|com.apple.finder)" | head -30
```


### .pi/node_modules tracked in git — FIXED 2026-07-12

Done: `git rm -r --cached .pi/node_modules` on 2026-07-12. No longer tracked.

## File Layout (current)

```
~/config/
  README.md                 # Quick reference
  flake.nix                 # Entry point
  flake.lock                # Pinned versions
  .gitignore                # Secrets, caches
  nix_secrets               # SSH env vars, API keys (GITIGNORED)

  starship.toml             # Prompt
  config.ghostty            # Terminal
  .aerospace.toml           # Window manager
  .tmux.conf                # Tmux
  lazygit/config.yml        # LazyGit
  zed/settings.json         # Zed editor
  sioyek/prefs_user.config  # PDF reader
  raycast-scripts/          # Raycast script extensions

  repolicense-cli/          # Git submodule

  nix/
    home.nix                # Home-manager entry (imports common/*)
    darwin.nix              # Nix-darwin entry (imports darwin/*)
    MIGRATION.md            # This file
    common/                 # Cross-platform home-manager modules
      packages/             # Nix packages (cli, languages, data, cloud, media, apps, extras)
      shell/                # Zsh config (init, aliases, paths, completions, env, starship)
      git.nix               # Git, gh, GPG
      tmux.nix              # Tmux binary
      vim.nix               # Vim + Catppuccin theme
      herdr.nix             # herdr terminal workspace manager config
      ssh.nix               # SSH client config, host aliases from nix_secrets
      dotfiles.nix          # Symlinks for all dotfiles
    darwin/                 # macOS-only nix-darwin modules
      system.nix            # macOS defaults
      hostname.nix          # Hostname, user, Nix daemon, GC
      launchd.nix           # launchd agents (borders, tailwind cleanup)
      services.nix          # Nix services stubs (postgres, redis)
      homebrew/             # Brew management
        brews.nix           # Brew formulas
        casks.nix           # Brew casks (GUI apps)
        mas.nix             # Mac App Store apps
        activation.nix      # Brew trust + cleanup scripts

  # Gitignored (on disk, not in repo):
  nix_secrets               # SSH aliases, API tokens
  .claude/.credentials.json # Claude auth
  .claude/anthropic_key.sh  # Claude API key
  .omp/agent/*.db*          # Agent databases
  ~/.zshrc_local            # Quick alias experiments (no rebuild)
```
