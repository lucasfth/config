# AGENTS.md — Nix Config Repo

This is a nix-darwin + home-manager configuration repo for macOS (Apple Silicon, `aarch64-darwin`). Every tool, dotfile, brew package, and macOS setting is declaratively managed here.

<critical>
Vault tech docs are auto-injected into every session as user context. They ARE authoritative and you MUST apply them to all changes in this repo:
- `tech/030-Coding-Style.md` — universal coding rules (commits, bug fixes, deps, over-engineering)
- `tech/032-Nix-Darwin-Patterns.md` — Nix service definitions, theming, file changes, common pitfalls
These are non-negotiable. If any instruction conflicts with them, the vault doc wins.
</critical>

## Memory & Context

- **Session start:** Vault context auto-injected by `~/.omp/agent/hooks/vault.ts` — always includes `tech/030-Coding-Style.md` plus stack-specific tech docs (e.g. `tech/032-Nix-Darwin-Patterns.md` for this repo), then recent session notes from `~/vault/projects/`.
- **Session end:** A stub file is auto-created at `~/vault/projects/<org>/<repo>/<branch>/YYYY-MM-DD.md`.
  Before the session ends, append a summary to that file.
  Use the `bash` tool to append: `cat >> ~/vault/projects/... << 'EOF'`
## Architecture

```
flake.nix (entry) → darwin.lib.darwinSystem
  ├── nix/darwin.nix              imports darwin/* modules
  │   ├── darwin/system.nix       macOS defaults (dock, finder, trackpad)
  │   ├── darwin/hostname.nix     hostname, user, nix daemon, GC
  │   ├── darwin/launchd.nix      launchd agents (borders, tailwind cleanup)
  │   ├── darwin/services.nix     Nix services stubs (postgres, redis)
  │   └── darwin/homebrew/        brew taps, brews, casks, mas, activation
  └── home-manager (integrated)
      └── nix/home.nix            imports common/* modules
          ├── common/packages/    CLI tools from nixpkgs (8 sub-modules)
          ├── common/shell/       zsh config split into init/aliases/paths/completions/env
          ├── common/git.nix      git config, gh, GPG, gpg-agent
          ├── common/tmux.nix     tmux binary
          ├── common/vim.nix      vim config + catppuccin theme
          ├── common/herdr.nix    herdr terminal workspace manager config
          ├── common/ssh.nix      SSH client config, host aliases from nix_secrets
          └── common/dotfiles.nix symlinks for dotfiles (starship, ghostty, tmux, zed, etc.)
```

## Key facts

- **Hostname:** `lucas-macbook-pro` (hardcoded in flake.nix + hostname.nix)
- **Username:** `lucasfreytorreshanson`
- **Repo path:** `~/config` (symlinked from `~/Desktop/code/config`)
- **nix-darwin generations:** `sudo darwin-rebuild --list-generations`
- **Brew is declarative:** `nix/darwin/homebrew/` manages brew. `nrs` auto-updates brew packages. Never run `brew install` directly.
- **Shell:** Nix-managed zsh at `~/.nix-profile/bin/zsh`. `.zshrc` is GENERATED by home-manager from `common/shell/` — never edit `.zshrc` directly.
- **Secrets:** `~/config/nix_secrets` (gitignored) sourced at shell init. Also `.claude/.credentials.json`, `.claude/anthropic_key.sh`.
- **GPG:** Key `E8DE72E441853146`, gpg-agent managed by launchd via home-manager.

## CLI shortcuts (defined in common/shell/init.nix)

| Command | Does |
|---------|------|
| `nrs` | Rebuild system + reload shell |
| `nix-search <pkg>` | Search nixpkgs |
| `nix-which <cmd>` | Show path + Nix/Brew source |
| `nix-update` | Update flake.lock + rebuild + reload |
| `nix-rollback` | List generations, show rollback cmd |

## Making changes

- **Add Nix package:** edit `nix/common/packages/<category>.nix` → `nrs`
- **Add Brew package:** edit `nix/darwin/homebrew/brews.nix` (formula) or `casks.nix` (cask) → `nrs`
- **Change zsh config/aliases:** edit `nix/common/shell/<file>.nix` → `nrs`
- **Change dotfile:** edit the file (starship.toml, config.ghostty, etc.) → `nrs`
- **Quick alias test (no rebuild):** `echo 'alias ...' >> ~/.zshrc_local` → `exec zsh`
- **Change macOS setting:** edit `nix/darwin/system.nix` → `nrs`
- **Change git/GPG:** edit `nix/common/git.nix` → `nrs`

## What NOT to do

- **Never** `brew install` — brew is declarative via nix/darwin/homebrew/
- **Never** commit `nix_secrets`, `.claude/.credentials.json`, `node_modules/`
- **Never** run `darwin-rebuild` without `sudo` after the initial bootstrap
- **Never** remove `nixpkgs.config.allowUnfree = true` from home.nix (needed for slack)

## Remaining TODOs (see MIGRATION.md)

- Postgres & Redis: deferred — nix-darwin service modules immature (no auto initdb, no redis.conf generation)
- yt-dlp: manual clone at ~/Desktop/code/, not in Nix

## Bootstrap (new machine)

```bash
# Install Nix, enable flakes, install Homebrew
git clone git@github.com:lucasfth/config.git ~/config
cd ~/config
touch nix_secrets
# Edit hostname.nix + flake.nix + home.nix for new hostname/username
nix build .#darwinConfigurations.<hostname>.system
./result/sw/bin/darwin-rebuild switch --flake .
sudo chsh -s ~/.nix-profile/bin/zsh $USER
```
